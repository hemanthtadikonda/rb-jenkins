pipeline {
   agent any
   environment {
      URL = 'https://google.com'                                                                  google.com'
   }
   stages {
      stage('compile') {
         steps {
            echo 'code compile'
         }
      }
      stage('test') {
         steps {
            echo URL
         }
      }
   }
}